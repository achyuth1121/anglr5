import { SeMasterContetHeightDirective } from './se-master-contet-height.directive';

describe('SeMasterContetHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new SeMasterContetHeightDirective();
    expect(directive).toBeTruthy();
  });
});
